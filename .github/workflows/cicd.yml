name: CI/CD Pipeline

on:
  push:
    tags: ["v[0-9]*"]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  UV_PYTHON: "3.13"

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Check formatting with ruff
        run: uv run --locked ruff format --check
        continue-on-error: true

      - name: Lint with ruff
        run: uv run --locked ruff check

      - name: Type check with basedpyright
        run: uv run --locked basedpyright

  pypi:
    name: Publish to PyPI
    if: startsWith(github.ref, 'refs/tags/v')
    needs: lint
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/project/pyscribest
    permissions:
      contents: read
      id-token: write
      attestations: write
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Build source & wheel
        run: uv build --no-create-gitignore

      - name: Smoke test
        run: |
          uv run --isolated --no-project --with dist/*.whl pyscribe --version
          uv run --isolated --no-project --with dist/*.whl python -m pyscribe --version
          uv run --isolated --no-project --with dist/*.tar.gz pyscribe --version

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1

  build:
    name: Build binaries
    if: startsWith(github.ref, 'refs/tags/v')
    needs: lint
    permissions:
      contents: read
      id-token: write
      attestations: write
    uses: ./.github/workflows/build-binaries.yml

  release:
    name: Release on GitHub
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all binaries
        uses: actions/download-artifact@v7
        with:
          merge-multiple: true

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            *
          generate_release_notes: true
